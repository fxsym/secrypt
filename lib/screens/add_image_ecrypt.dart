import 'package:flutter/material.dart';

class AddImageEncryptScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Tambah Gambar Enkripsi")),
      body: Center(
        child: Text("Halaman Dummy Tambah Gambar Enkripsi"),
      ),
    );
  }
}
