import 'package:flutter/material.dart';

class AddTextEncryptScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Tambah Text Enkripsi")),
      body: Center(
        child: Text("Halaman Dummy Tambah Text Enkripsi"),
      ),
    );
  }
}
